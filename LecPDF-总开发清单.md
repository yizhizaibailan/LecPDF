# LecPDF 总开发清单

> 状态基线：2026-08-30，代码基线 `6f58213`。本文件是整个软件的长期进度看板；完成一项功能后，必须先验证、提交并推送，再将对应复选框改为 `[x]`。

## 使用规则

- `[x]`：已经有代码、自动化验证通过并已提交到远端分支。
- `[-]`：正在进行；一个提交只允许推进当前相关条目。
- `[ ]`：尚未开始，或仍被前置风险验证阻塞。
- 专项实施细节见 [目录重构与双阅读引擎清单](docs/superpowers/plans/2026-08-30-lecpdf-structure-and-reader-engines.md)。
- PDF 必须使用 **EmbedPDF**；EPUB 与后续非 PDF 电子书必须使用 **foliate-js**，不得在同一阅读器中混用两个内核。
- 所有既有及新增的 TypeScript/TSX 模块均须按设计文档补充中文注释：文件职责、导出项用途，以及 IPC、异步、资源释放和引擎适配等关键逻辑的实现原因。

## 0. 架构、质量与工程规范

- [x] 建立覆盖整个软件范围、可持续勾选的总开发清单。
- [x] 建立 Electron + React + TypeScript 项目骨架。
- [x] 建立 Ant Design 主题与 Solar 图标基础。
- [x] 建立共享 schema、IPC 契约和 preload 安全 API 骨架。
- [x] 建立 Vitest、TypeScript 检查和 Electron-Vite 生产构建质量门禁。
- [-] 按参考截图完成 `electron / engines / src` 模块化目录重构。
- [ ] 将单体 `App.tsx` 拆为布局、页面、阅读器、路由、状态和可复用组件。
- [ ] 建立中英文文案资源与 i18n 热切换。
- [ ] 为所有既有与新增 TypeScript/TSX 文件补齐中文职责与实现注释。
- [ ] 建立注释覆盖检查脚本，并纳入常规验证。
- [ ] 建立持续集成：测试、类型检查、构建和格式/路径检查。

## 1. 本地数据、稳定性与系统能力

- [x] 实现 userData JSON 原子读写、并发保护和损坏文件错误处理。
- [x] 实现 schema 迁移链和未知版本只读保护。
- [x] 实现配置、侧栏 sidecar 的安全路径映射与 IPC 读写。
- [x] 实现无边框窗口、最小化/最大化/关闭控制与窗口几何恢复。
- [x] 实现单实例启动和命令行文件路由。
- [x] 实现受限 `lec-file` PDF 流式协议。
- [x] 实现“我的文档”文件夹扫描服务。
- [x] 实现异常退出标记与恢复所需的运行时记录。
- [x] 实现自动备份服务、手动数据导出与安全导入基础能力。
- [x] 实现本地日志与手动检查更新服务。
- [ ] 实现 500ms 防抖写盘编排与损坏 sidecar 隔离提示。
- [ ] 实现缓存占用统计、2GB LRU 与设置页清理缓存。
- [ ] 实现“删除文件到系统回收站”及二次确认流程。
- [ ] 完成崩溃恢复的真实应用启动/强制退出人工验收。
- [ ] 完成零遥测与本地日志不含文档内容的发布前审计。

## 2. 应用外壳、标签页与开始页

- [ ] 实现常驻、不可关闭的开始页标签。
- [ ] 实现多文档标签页：打开、切换、关闭、中键关闭、Ctrl+W、溢出滚动和最多 20 个标签限制。
- [ ] 实现应用菜单：文件、编辑、视图、窗口、设置、帮助。
- [ ] 实现开始页的最近、星标、阅读位置、我的文档、书签与标注分区。
- [ ] 实现打开文件按钮、窗口拖放和系统文件关联三种入口。
- [ ] 实现文件移动后的失效状态、重新定位和提示。
- [ ] 实现最近记录（最多 100 条、LRU 淘汰、进度显示）与全局星标同步。
- [ ] 实现我的文档文件夹添加、刷新扫描、移出库和回收站删除操作。
- [ ] 实现跨文件书签与批注聚合视图、筛选、搜索和跳转定位。

## 3. PDF 阅读（EmbedPDF）

- [x] 明确采用 EmbedPDF 作为 PDF 唯一阅读内核。
- [x] 实现 PDF 打开、受限本地文件读取和渲染视口挂载。
- [x] 实现单页、连续和双页布局切换。
- [x] 实现页码跳转、上一页/下一页、缩放、适页与视图旋转。
- [x] 实现 PDF 缩略图懒加载侧栏和目录树跳转。
- [x] 实现 PDF 文档内搜索、结果计数、上下项跳转和大小写匹配。
- [ ] 实现 PDF 工具栏与侧栏在重构后保留全部已完成功能。
- [ ] 实现 Ctrl+滚轮缩放、PgUp/PgDn/方向键翻页与 F11 全屏阅读。
- [ ] 实现加密 PDF 密码输入、错误重试和仅会话内记住密码。
- [ ] 实现文件级阅读进度、缩放与位置恢复。
- [ ] 实现 PDF 书签：命名、排序、删除、选区/页码定位和侧栏展示。
- [ ] 完成大文件轻量模式、非活动标签缓存释放和性能测量。

## 4. PDF 批注、夜间模式与打印

- [x] 建立七类统一批注 schema、sidecar 操作和坐标转换基础。
- [x] 完成 R0-02：验证 PDF 批注打印路线，结论为 sidecar + 应用层 PrintComposer。
- [ ] 实现 PDF 高亮、下划线、删除线、波浪线、便签、自由文本和手绘墨迹的创建与渲染。
- [ ] 实现七类批注的编辑、删除、按页侧栏列表、颜色预设与点击定位。
- [ ] 实现批注实时保存、重开恢复及会话级 50 步撤销/重做。
- [ ] 实现旋转 0/90/180/270 度下的批注坐标对齐回归测试。
- [ ] 实现原文打印与“原文 + 批注层”打印，提供页码范围和缩放选项。
- [ ] 在开发 PDF 智能反色前完成 R0-03 分层渲染验证。
- [ ] 根据 R0-03 结论实现 PDF 夜间模式及批注对比度保护；不可行时明确降级方案。

## 5. 电子书阅读（foliate-js）

- [x] 确认采用 foliate-js 作为非 PDF 电子书的唯一阅读内核。
- [ ] 安装并封装 foliate-js，建立与通用 ReaderEngine 对接的适配层。
- [ ] 实现 EPUB 的受限字节读取、打开、关闭和阅读位置保存。
- [ ] 实现 EPUB 分页、滚动和双页布局。
- [ ] 实现 EPUB 章节目录、当前章节高亮和跳转。
- [ ] 实现 EPUB 全书搜索、结果高亮和章节跳转。
- [ ] 实现每本书独立的字号、行距、页边距、浅色/暖黄/深色主题设置。
- [ ] 实现 EPUB 书签、命名、排序、侧栏展示和跳转。
- [ ] 在开发 EPUB 批注前完成 R0-04：Foliate 选区锚定、重开恢复和文本回找验证。
- [ ] 根据 R0-04 结论实现 EPUB 文本批注及 orphaned/ambiguous 显式状态。
- [ ] 在开发 TTS 前完成 R0-05：句级切分、系统语音与高亮同步验证。
- [ ] 根据 R0-05 结论实现 TTS：语速、音色、播放/暂停/停止、跨章和句级高亮。
- [ ] 逐项评估 foliate-js 支持的其他格式后再开放扩展名；不承诺未验证格式。

## 6. 通用阅读功能与交互

- [ ] 统一 PDF 与 EPUB 的阅读会话、错误态、加载态和文档类型分发。
- [ ] 实现通用书签数据模型与跨格式定位。
- [ ] 实现通用批注数据模型与聚合筛选（在各引擎数据源可用后）。
- [ ] 实现完整默认快捷键：打开、关闭、搜索、翻页、缩放、批注、撤销/重做、全屏、打印。
- [ ] 实现快捷键自定义、冲突检测与恢复默认。
- [ ] 实现复制文本、右键/选区工具条和可访问性键盘焦点。
- [ ] 完成浅色、深色、跟随系统三种应用外观主题。

## 7. 设置、帮助与产品完整性

- [ ] 实现设置页“通用”：语言、启动项、更新、缓存、导入导出、自动备份。
- [ ] 实现设置页“阅读”：默认缩放、布局、PDF 夜间模式与翻页动画。
- [ ] 实现设置页“外观”：浅色、深色、跟随系统。
- [ ] 实现设置页“批注”：七类批注的默认颜色。
- [ ] 实现设置页“快捷键”：动作列表、改键、冲突提示与恢复默认。
- [ ] 实现帮助菜单：快捷键总览、使用文档、检查更新和关于。
- [ ] 实现关于页：版本、EmbedPDF/foliate-js/Electron 等引擎信息及开源许可声明。

## 8. 安装、性能与发布（Windows v1）

- [ ] 配置 Windows 安装包、升级与卸载流程。
- [ ] 注册 `.pdf` 与已支持电子书格式的可选文件关联。
- [ ] 验证双击文件时路由到已有单实例的新标签。
- [ ] 建立 ≤200MB / ≤2000 页 PDF 的打开、翻页、滚动性能基线与人工测试记录。
- [ ] 验证单标签内存峰值、后台标签资源释放和长时间阅读稳定性。
- [ ] 验证强制退出后最多丢失 500ms 操作、备份恢复和导入导出一致性。
- [ ] 完成 Windows 10/11 安装、打开、阅读、批注、打印、更新与卸载冒烟测试。
- [ ] 发布前复核：所有已勾选条目都有对应提交、验证记录和用户可见结果。

## 9. v1 后路线（不计入当前完成度）

- [ ] PDF 批注写回原文件/导出含批注 PDF。
- [ ] 跨文件全文搜索。
- [ ] 多窗口和无条件会话恢复。
- [ ] macOS、Linux 版本。
- [ ] 云端同步、账号体系与 AI 功能（均不属于当前 v1 范围）。

## 当前执行位置

- 当前阶段：**0. 架构、质量与工程规范**。
- 当前任务：**目录重构与双阅读引擎接入**（详见专项清单 Task 1）。
- 下一提交目标：迁移构建入口到 `electron/`、`engines/` 与根目录 `src/`，并保持测试、类型检查和构建通过。
