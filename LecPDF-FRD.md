# LecPDF 功能需求文档（FRD）

> 版本：v1.0（对齐稿） · 日期：2026-08-27
> 来源：墨刀在线分享原型 + 离线导出包 + UPDF 参考 + 对齐访谈（40 项决策）

---

## 1. 项目概述

LecPDF 是一款桌面端 PDF / EPUB 阅读器，主打**阅读与批注**。视觉与交互参考墨刀原型图（仿桌面窗口、标签页式多文档、浅色底 + slate/blue 主色），界面体系使用 **Ant Design**，产品形态参考 UPDF 的布局结构。

### 1.1 产品定位

| 项 | 决策 |
|---|---|
| 形态 | 桌面应用（Electron 壳 + Web 渲染层） |
| 内核 | PDF：embedpdf（PDFium-WASM）；EPUB：epub.js |
| 功能边界 | 阅读 + 批注（不做编辑/转换/OCR/AI） |
| 界面语言 | 中文 + 英文（i18n） |
| 平台策略 | 目标全平台；v1 先交付 Windows |
| 数据隐私 | 零遥测；本地崩溃日志，仅手动上报 |

---

## 2. 范围

### 2.1 范围内（v1）

- 开始页（文件枢纽）、多标签页阅读、PDF/EPUB 双引擎
- PDF 批注 7 类、EPUB 批注、书签、文档内搜索、聚合视图
- EPUB 阅读设置、TTS 朗读、夜间模式、打印
- 设置页（5 组）、自定义快捷键、数据导出与自动备份
- 文件关联、加密 PDF 打开、崩溃恢复、自动更新检查

### 2.2 非目标（明确不做）

| 项 | 说明 |
|---|---|
| PDF 页面编辑 / 图片文字编辑 | UPDF 式编辑能力，超出 embedpdf |
| 格式转换 / 压缩 | 超出边界 |
| OCR / AI 总结翻译 | 需后端，超出边界 |
| 云端同步 / 账号体系 | v1 纯本地 |
| 全局跨文件搜索 | 进 roadmap P2 |
| 多窗口模式 | 进 roadmap P2 |
| 会话级标签恢复（无条件） | 进 roadmap P2；v1 仅崩溃后提示恢复 |
| 无痕阅读模式 | 已否决 |

### 2.3 风险项（标注：引擎能力待验证）

| 项 | 风险 |
|---|---|
| 下划线 / 删除线 / 波浪线批注 | embedpdf 未公开声明支持，可能需扩展引擎或自绘 |
| PDF 智能反色（文字反色、图片保真） | 依赖 embedpdf 分层渲染能力；不支持则退化为整体反色 |
| EPUB 选区锚定批注 | epub.js 选区定位需自研锚定逻辑 |
| 句级高亮跟读（TTS） | 需动 epub.js 渲染层 |

---

## 3. 决策记录（对齐结论摘要）

1. 形态：Electron 桌面端（预留换壳，渲染层不依赖 Electron 专属 API）
2. 边界：阅读 + 批注；UPDF 仅作 UI/UX 参考，不做其编辑/转换/AI
3. 开始页：完整文件管理（打开/最近/星标/阅读位置/我的文档/书签与标注），砍"新建"
4. 打开模型：单窗口多标签页
5. EPUB：v1 双引擎（embedpdf + epub.js）
6. 批注类型：高亮、便签、自由文本、手绘、下划线、删除线、波浪线（后 3 类为风险项）
7. 批注存储：sidecar（userData 下 JSON），不改写原文件；P2 补"导出/回写 PDF"
8. 设计体系：Ant Design；整体风格按原型图（frameless 桌面质感）
9. 搜索：仅文档内搜索
10. 侧栏：PDF=缩略图/目录/书签/批注；EPUB=目录/书签/批注
11. 进度恢复：文件级位置记忆（页码/章节 + 缩放 + 滚动偏移）
12. 阅读布局：PDF 单页/连续/双页；EPUB 分页/滚动/双页
13. EPUB 阅读设置：字号/行距/页边距/三主题，按书记忆
14. 窗口框架：frameless 自定义标题栏（图标+菜单+标签+窗口控制三键）
15. 平台：Windows 先行（v1），macOS/Linux 进 roadmap
16. 语言：中英双语 v1（i18next 框架）
17. 文件关联：注册 .pdf/.epub，单实例路由新标签
18. 加密 PDF：支持打开密码，会话内记住，不持久化
19. PDF 夜间模式：智能反色（风险项见 2.3）
20. 打印：完整打印（原始内容+批注层可选、页码范围、缩放）
21. 快捷键：完整默认体系 + 设置页自定义改键（含冲突检测）
22. 设置页：5 组（通用/阅读/外观/批注/快捷键）
23. 数据目录：userData；"我的文档"=用户手动添加文件夹扫描（不复制文件）
24. 更新：检查提示手动更新（electron-updater）
25. 性能目标：≤200MB / ≤2000 页流畅；超限降级打开
26. 批注撤销/重做：会话级（≤50 步，不跨重启）
27. TTS：完整版（语速/音色/句级高亮/多语言）
28. 聚合视图：跨文件书签 + 批注列表，可跳转定位
29. 数据导出：手动导出/导入 + 定时自动备份
30. 删除文件：移出库（默认）+ 删除文件（确认，走系统回收站）
31. 书签：用户自建（命名/排序/跳转），与文件自带目录并存
32. 遥测：零遥测；崩溃日志本地化，手动上报
33. 小交互：拖放打开/全屏/页面旋转/复制文本/EPUB 选区批注
34. 写盘：实时自动保存（500ms 防抖）
35. 崩溃恢复：异常退出标记 + 启动提示恢复
36. 帮助菜单：快捷键总览/使用文档/检查更新/关于（含开源协议声明）
37. 渲染缓存：userData/cache，2GB LRU + 设置页清理
38. 窗口几何：记忆 bounds/最大化，越界回退主屏
39. 无痕模式：不做
40. 目录高亮：侧栏目录实时高亮当前章节（两侧引擎）

---

## 4. 功能需求

### 4.1 开始页（文件枢纽）

**4.1.1 布局**（对应原型 homePage）
- 左侧导航：最近 / 星标 / 阅读位置（我的文档、书签与标注）/ 本地（我的文档入口）
- 右侧内容区：当前分区列表 + 顶部搜索框（仅对当前分区过滤，非全文检索）
- 顶栏：全局应用菜单 + 标签页区（开始页作为常驻标签，不可关闭）

**4.1.2 文件打开**
- F1 入口一：开始页"打开"按钮 → 系统文件选择器（.pdf / .epub 过滤器）
- F2 入口二：拖放文件到窗口任意位置打开
- F3 入口三：系统文件关联双击 / 右键"用 LecPDF 打开"（单实例路由到新标签）
- F4 文件未找到（路径失效）：列表项标灰，点击提示"文件已移动，请重新定位"，支持手动重定位

**4.1.3 最近列表**
- 记录：文件路径、名称、最后打开时间、阅读进度（页/章 + 百分比）
- 上限 100 条，LRU 淘汰；星标文件置顶组
- 条目操作：打开、定位文件、移出最近、加入/取消星标

**4.1.4 星标**
- 任意文件可标星；开始页"星标"分区查看全部星标文件
- 星标状态全局唯一（同一文件在最近/我的文档中同步显示）

**4.1.5 我的文档**
- 用户手动添加文件夹（支持多个），应用扫描目录下 .pdf/.epub 建立索引（不复制文件）
- 索引元数据：路径、名称、大小、修改时间、页数（可选提取）
- 支持：手动刷新扫描、移出库、删除文件（确认后送系统回收站）、打开

**4.1.6 书签与标注（聚合视图）**
- 书签聚合：所有文件的书签列表（文件、页码/章节、命名、时间），点击 → 打开文件并定位
- 批注聚合：所有文件的批注列表，筛选（按文件 / 按批注类型），点击 → 打开文件并定位
- 均支持搜索过滤（对批注文本/书签名称）

### 4.2 窗口与标签页

- frameless 自定义标题栏：Logo 区（可点击回开始页）+ 应用菜单按钮 + 标签页区 + 窗口控制（最小化/最大化/关闭）
- 标签页：文档标签（图标+名称+关闭钮）、开始页标签常驻不可关；标签溢出横向滚动；中键关闭；Ctrl+W 关闭当前
- 标签页上限 20，超出提示先关闭其他标签
- 窗口几何记忆与恢复（越界回退主屏中央）
- 应用菜单：文件（打开/最近列表）、编辑、视图、窗口、设置、帮助（对应原型 appMenu）

### 4.3 PDF 阅读器

**4.3.1 渲染与导航**
- embedpdf 渲染；支持单页 / 连续滚动 / 双页三种布局，双页奇数页补白
- 翻页：滚动、PgUp/PgDn、方向键、工具栏页码输入框（"第 X / N 页"）
- 缩略图侧栏：点击跳页，当前页高亮，懒加载
- 目录侧栏：PDF outline 树形展示 + 当前位置高亮 + 点击跳转
- 缩放：10%–400%，Ctrl+滚轮、Ctrl+/-、适页/适宽按钮
- 页面旋转：视图级旋转（0/90/180/270），不写文件，不跨会话持久化
- 全屏阅读模式（F11 / 工具栏按钮），全屏下隐藏标题栏与工具栏

**4.3.2 搜索**
- Ctrl+F 打开搜索栏：关键词、命中计数、上一个/下一个、大小写敏感开关
- 结果高亮；点击跳转对应页；支持连续文档内全部命中

**4.3.3 批注（7 类）**
- 高亮、下划线、删除线、波浪线（风险项）、便签（sticky note）、自由文本、手绘墨迹
- 交互：工具栏激活工具 → 划选文本/点击落点；便签弹编辑浮层
- 颜色：每类 5 色预设（含默认色），可在设置页改默认色
- 批注侧栏：按页分组列表，条目显示类型图标/颜色/文本摘录/时间；点击定位；支持编辑（改色/改文）、删除
- 撤销/重做：Ctrl+Z/Y，会话级 50 步，仅限批注操作
- 存储：实时写入 sidecar（500ms 防抖），不改写原 PDF
- 加密 PDF 的批注同样存 sidecar（不要求保存密码）

**4.3.4 夜间模式**
- 智能反色：文字反白、图片尽量保真（风险项）；开关独立于 EPUB 主题
- 批注颜色在反色模式下保持可读（渲染时做对比度保护）

**4.3.5 打印**
- 系统打印对话框；选项：原始内容 / 含批注层（可选）、页码范围、缩放
- 带批注打印依赖"批注合成渲染"能力，与 P2 批注回写同线（风险项）

### 4.4 EPUB 阅读器

- epub.js 渲染；布局：分页 / 滚动 / 双页
- 目录侧栏：章节树 + 当前位置高亮 + 点击跳转；书签侧栏；批注侧栏
- 阅读设置（每本书独立记忆）：字号 14–23、行距 1.4–2.2、页边距 3–6、主题（浅色/暖黄/深色）
- 批注：选中文本 → 工具条（高亮/下划线/删除线/波浪线/便签/复制）；锚定逻辑见风险项
- 搜索：本书全文搜索，命中跳转章节
- TTS 朗读：完整版——语速调节、音色选择、句级高亮跟读、多语言引擎、暂停/停止/继续；进度显示
- 书签：添加当前章节书签（命名/排序）

### 4.5 书签（通用）

- PDF：收藏当前页/选区，可命名、排序；EPUB：收藏当前章节
- 侧栏书签列表：点击跳转定位；与文件自带目录（outline/章节树）独立并存

### 4.6 设置页（5 组）

| 分组 | 内容 |
|---|---|
| 通用 | 语言（中/英）、开机启动、更新通道（检查更新入口）、存储占用（缓存大小+清空按钮）、数据导出/导入、自动备份（开关+周期） |
| 阅读 | 默认缩放、默认布局模式（单页/连续/双页）、默认主题（PDF 夜间开关）、翻页动画开关 |
| 外观 | 应用主题（浅/深/跟随系统） |
| 批注 | 各类批注默认颜色 |
| 快捷键 | 全部动作列表 + 自定义改键 + 冲突检测 + 恢复默认 |

### 4.7 快捷键（默认）

| 动作 | 默认键 |
|---|---|
| 打开 | Ctrl+O |
| 关闭标签 | Ctrl+W |
| 搜索 | Ctrl+F |
| 翻页 | PgUp / PgDn / 方向键 |
| 缩放 | Ctrl + / - / 0（滚轮） |
| 撤销/重做 | Ctrl+Z / Ctrl+Y |
| 高亮 | H |
| 下划线 | U |
| 删除线 | D |
| 波浪线 | W |
| 便签 | N |
| 手绘 | P |
| 全屏 | F11 |
| 打印 | Ctrl+P |

全部可在设置页改键，冲突时拦截并提示。

### 4.8 数据管理

- 存储位置：`userData/`（Windows `%APPDATA%/LecPDF/`）：`config.json`、`library.json`（索引/最近/星标）、`data/<file-hash>.json`（sidecar 批注/书签/位置/EPUB 设置）、`cache/`（渲染缓存）、`backups/`（自动备份）、`logs/`（崩溃日志）
- 实时写盘（500ms 防抖）；崩溃最多丢失半秒内操作
- 导出：单文件 JSON（或 zip）打包全部批注/书签/进度；导入按路径匹配还原
- 自动备份：默认每周一次到 `backups/`，保留最近 3 份；设置页可关/可改周期
- 渲染缓存：2GB 上限 LRU；设置页显示占用并可清空

### 4.9 系统集成

- 文件关联：安装器注册 .pdf/.epub（用户可选不设默认）；命令行参数 `LecPDF <file>` 打开
- 单实例：二次启动将文件路由到已开窗口新标签
- 加密 PDF：打开时弹密码框，验证通过后阅读；会话内记住；不持久化明文密码
- 崩溃恢复：检测异常退出标记，下次启动提示"恢复上次打开的文档？"
- 更新：手动检查更新（帮助菜单 + 设置页），提示后手动下载
- 隐私：零遥测；本地崩溃日志，用户手动上报（帮助菜单可打开日志目录）

### 4.10 帮助 / 关于

- 快捷键总览（跳设置页）、使用文档（内置说明或官网链接）、检查更新、关于
- 关于页：版本号、引擎信息、开源协议声明（embedpdf Apache-2.0 / epub.js 等）

---

## 5. 非功能需求

| 项 | 指标 |
|---|---|
| 性能 | ≤200MB / ≤2000 页 PDF：打开 ≤3s、翻页首屏 ≤300ms、滚动 60fps 目标 |
| 性能降级 | >200MB 文件警告后尝试打开：关闭预渲染、缩略图懒加载，不承诺流畅度 |
| 稳定性 | 崩溃率 < 0.1%；数据零丢失（实时写盘 + 自动备份） |
| 内存 | 单标签 PDF 渲染峰值 ≤ 500MB；标签后台化释放非活动文档缓存 |
| 安全 | 零遥测；不记录文件内容；密码不落盘；删除走系统回收站 |
| 合规 | 开源协议声明完整（Apache-2.0 组件） |
| 兼容 | v1 Windows 10/11（x64）；架构保持平台无关（macOS/Linux roadmap） |

---

## 6. Roadmap（超出 v1）

| 优先级 | 项 |
|---|---|
| P1（风险项先行验证） | 下划线/删除线/波浪线、PDF 智能反色、EPUB 选区锚定、TTS 句级高亮 |
| P2 | 批注导出/回写 PDF（含带批注打印的合成渲染）、全局跨文件搜索、多窗口、无条件会话恢复、macOS 版、自定义改键导入导出 |
| P3 | Linux 版、Tauri 换壳评估、云同步、AI 辅助 |

---

## 7. 待验证清单（开发前必须做的事）

1. embedpdf 是否支持 Underline/Strikeout/Squiggly 批注类型（决定风险项落地方式）
2. embedpdf 分层渲染能力（夜间模式智能反色可行性）
3. embedpdf 带批注合成导出（打印/回写 PDF 路线）
4. epub.js 选区锚定与句级高亮可行性
5. embedpdf v2 稳定版 vs v3 开发版选型（v3 未推荐生产）
